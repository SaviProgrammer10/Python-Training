n = input("Enter a number: ")

mid = len(n) // 2

if len(n) % 2 == 0:
    product = int(n[mid - 1]) * int(n[mid])
    print("Product of middle digits:", product)
else:
    print("Middle digit:", n[mid])
